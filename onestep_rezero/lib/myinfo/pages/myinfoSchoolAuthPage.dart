import 'package:flutter/material.dart';

class MyinfoSchoolAuthPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("대학교인증"),
      ),
      body: Container(
        child: Text("gdgd"),
      ),
    );
  }
}
