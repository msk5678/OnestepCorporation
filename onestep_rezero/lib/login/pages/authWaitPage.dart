import 'package:flutter/material.dart';

class AuthWaitPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text("증명서 인증 대기중입니다"),
        ),
      ),
    );
  }
}
