import 'package:flutter/material.dart';

import 'kakaoTest.dart';

class KaKaoTestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("cex"),
      ),
      body: LinkScreen(),
    );
  }
}
